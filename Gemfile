# 声明 RubyGems 源（Ruby 包仓库）
source "https://rubygems.org"

# 引入 GitHub Pages 官方依赖（固定版本，避免与 GitHub 服务器冲突）
gem "github-pages", group: :jekyll_plugins

# Jekyll 插件组（GitHub Pages 会自动加载这个组的插件）
group :jekyll_plugins do
  # 添加 Mermaid 支持插件
  gem "jekyll-mermaid"
end